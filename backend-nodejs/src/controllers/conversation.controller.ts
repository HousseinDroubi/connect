import { Request, Response } from "express";

const deleteConversation = (request: Request, response: Response) => {
  // deleteConversation
};

const restoreConversation = (request: Request, response: Response) => {
  // getConversationMessages
};

const getConversationMessages = (request: Request, response: Response) => {
  // getConversationMessages
};

export { deleteConversation, restoreConversation, getConversationMessages };
